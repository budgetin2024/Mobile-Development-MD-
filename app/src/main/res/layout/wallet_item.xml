<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="200dp"
    android:layout_height="120dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <!-- Total Balance Label -->
        <TextView
            android:id="@+id/walletBalanceLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Total Balance"
            android:textColor="#000000"
            android:textSize="12sp" />

        <!-- Balance Value -->
        <TextView
            android:id="@+id/walletBalanceValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/walletBalanceLabel"
            android:layout_marginTop="4dp"
            android:text="Rp. 470.000.000"
            android:textColor="#000000"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Wallet Icon -->
        <ImageView
            android:id="@+id/walletIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="8dp"
            android:src="@drawable/ic_wallet"
            android:contentDescription="Wallet Icon" />

        <!-- Wallet Detail (Optional: like name or masked number) -->
        <TextView
            android:id="@+id/walletDetail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:text="**** 3456"
            android:textColor="#666666"
            android:textSize="12sp" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>
